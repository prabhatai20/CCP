#include<stdio.h>
#include<conio.h>
float main()
{
    float a,i,multi;
    printf("Enter two Numbers");
    scanf("%f %f",&a,&i);
    multi=a*i;
    printf("Multiplication of two floating Numbers %f and %f is %f",a,i,multi);
    return 0;
    getch();
}

